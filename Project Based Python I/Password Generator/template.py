import secrets
import string

def create_pw:
    # Use ascii letters, didits and punctuation for generating password.

    while not pw_strong:
        # Use secret module to generate strong password.
        # Your password must have special characters
        # Your password at least must have two digits.
        # Check if the password is strong.

if __name__ == '__main__':
    print(create_pw())